# import * from './generated/prisma.graphql'

type Mutation {
  createFilter(name: String, filterTerms: [String]): Filter!
  deleteFilter(id: ID!): Filter
}

type Query {
  filters: [Filter]!
  filter(where: FilterWhereUniqueInput!): Filter
}
