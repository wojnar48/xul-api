# Defines the schema for Prisma and the DB that lives under Prisma

type User {
  id: ID! @unique
  username: String!
  email: String! @unique
}

type SearchItem {
  id: ID! @unique
  text: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}
