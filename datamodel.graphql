# Defines the schema for Prisma and the DB that lives under Prisma

type User {
  id: ID! @unique
  username: String!
  email: String! @unique
}

type Filter {
  id: ID! @unique
  name: String!
  filterTerms: [String]!
  createdAt: DateTime!
  updatedAt: DateTime!
}
